#ifndef MAIN_H
#define MAIN_H
#include <stddef.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);

#endif
